# Технический долг — МАВИТА-ШОП

Дата ревью: 2026-06-28. Реестр замечаний по итогам критического ревью кода
Первоначальный реестр был создан перед Ф4; ниже статусы актуализированы после
реализации обоих компонентов в репозитории. Источник rollout-статуса —
[ROADMAP.md](../ROADMAP.md).

Легенда: ✅ исправлено · 🔜 отложено в фазу · 💤 осознанно отложено (minor).

---

## Блокеры (исправлены в этом заходе)

| ID | Замечание | Где | Статус |
| --- | --- | --- | --- |
| TD-1 | **IDOR**: заказ открывался по серийному `id` (`/order/1,2,3…`) без авторизации — перебором утекали email и состав чужих заказов (152-ФЗ). | `orders` (новый `token`), `getOrderByToken`, маршрут `/order/[token]`, success/fail | ✅ заказ адресуется неугадываемым `token` (UUID) |
| TD-2 | Страница заказа показывала «Оплачено» по query-параметру `?paid=1`, а не по статусу в БД — можно было выдать неоплаченный заказ за оплаченный. | `app/order/[token]/page.tsx` | ✅ баннер «оплачено» только при `status='paid'` |
| TD-3 | Платёжный слой (самый критичный) был без тестов, хотя ROADMAP заявлял покрытие I1/I3/I4. | `lib/robokassa.test.ts`, `app/api/robokassa/result.test.ts` | ✅ 14 тестов: подпись, result happy/битая/повтор/несовпадение суммы |
| TD-4 | `result` не сверял `OutSum` с суммой заказа — недоплата/рассинхрон прошли бы как оплата. | `markOrderPaid`, `app/api/robokassa/result/route.ts` | ✅ статус `paid` только при совпадении копеек; иначе 400 + лог |
| TD-5 | UI обещал «Подтверждение отправлено на email», но мейлера в проекте нет — врали покупателю. | `app/order/[token]/page.tsx` | ✅ формулировка убрана (реальные уведомления — см. TD-6) |
| TD-8 | Мёртвый дубль `POST /api/orders` создавал заказы в обход `init` — лишняя поверхность. | удалён | ✅ |

---

## Отложено в Ф4 (админка) / Ф5 (деплой)

| ID | Замечание | Куда | Примечание |
| --- | --- | --- | --- |
| TD-6 | **Частично закрыто:** уведомления владельцу уже есть через Telegram outbox, но покупатель по-прежнему не получает письмо/сообщение о заказе. | коммуникации | Операционный минимум для владельца реализован. Если покупательские уведомления понадобятся как часть UX или комплаенса, это уже отдельная фича, а не блокер текущего checkout. |
| TD-25 | **Outbox участвует в транзакции заказа.** Если `readNotificationSnapshot` или вставка в `order_notification_outbox` падают не из-за дедупликации, атомарно откатывается и создание/оплата заказа. | Telegram notifications | Осознанно: durability события важнее best-effort. `ON CONFLICT DO NOTHING` не роняет платёж; перед rollout обязательны миграция `004`, live PostgreSQL smoke-test и мониторинг runner-а. |
| TD-16 | Историческое замечание: админ-панель и I5 отсутствовали. | Ф4 | ✅ закрыто в репозитории: CRUD, upload, заказы, delivery settings и audit реализованы. Production rollout К2 отслеживается в ROADMAP. |
| TD-7 | Робокасса в тест-режиме требует **отдельные тестовые пароли**; код читает те же `ROBOKASSA_PASSWORD1/2`. | Ф5 | Соглашение задокументировано в `.env.example`. Перед Паузой 1 проверить, что в `.env` лежат правильные пароли под режим. |
| TD-14 | 54-ФЗ: продажа физлицам обычно требует фискальный чек; Робокасса может требовать параметр `Receipt`. | до Паузы 1 | Бизнес/юридический вопрос (самозанятый — чеки через «Мой налог»). Заложить до приёма реальных денег. |
| TD-26 | **Автоотправка СДЭК без автотестов.** Есть код и UI для `cdek-shipment`, `cdek-outbox`, webhook и waybill refresh, но в репозитории пока нет отдельных route/unit/integration-тестов на этот контур. | delivery rollout | Перед подтверждением production rollout нужны хотя бы happy-path и failure-path тесты на settings API, webhook, retry/requeue и drain-воркер. Сейчас этот хвост уже отмечен и в `TESTING_PLAN.md` как `FR-SET-2 ⬜`. |

---

## Осознанно отложено (minor)

| ID | Замечание | Примечание |
| --- | --- | --- |
| TD-9 | `inv_id` всегда равен `id` — отдельная колонка + лишний `UPDATE`. | Оставлено намеренно: явный InvId для Робокассы. Документировано в схеме. |
| TD-10 | Дубли одного `slug` в `items` не схлопывались в `buildOrderLines` → два `order_items` на товар. | ✅ исправлено: позиции мёржатся по slug (+тест). |
| TD-11 | `fetchCatalog` в `orders.ts` «дублирует» выборку из `lib/catalog.ts`. | By design: order-путь берёт минимальную проекцию (slug/name/price/in_stock) без LATERAL-джойнов фото — отдельный запрос оправдан. Не чиним. |
| TD-12 | Сравнение подписи через `===`, не `crypto.timingSafeEqual`. | ✅ исправлено: `timingSafeEqual` в `verifyResultSignature`. |
| TD-13 | Историческое замечание: `updated_at` не обновлялся автоматически. | ✅ закрыто для `products`: миграция `002` и `schema.sql` создают `trg_products_set_updated_at`. |
| TD-15 | Нет rate-limiting на создание заказа — можно наспамить `pending`. | Для низкого трафика приемлемо; вернуться при росте. |

---

## Фрод-ревью платёжного пути (перед приёмом реальных денег)

Дата: 2026-06-20. Ревью векторов мошенничества/потери денег по реальному коду
(`lib/robokassa.ts`, `lib/orders.ts`, `app/api/robokassa/*`, `app/order/[token]`).

**Базовая защита в порядке** (не дублируем): цена из каталога БД, а не от клиента;
подпись `Password2` проверяется на сервере timing-safe; сумма колбэка сверяется с
заказом; идемпотентность повторного колбэка; «оплачено» рисуется только по статусу БД;
доступ к заказу по неугадываемому `token`. См. TD-1…TD-4, TD-12.

| ID | Замечание | Где | Серьёзность / статус |
| --- | --- | --- | --- |
| TD-17 | **Оплата отменённого заказа «теряется».** При `status='cancelled'` `markOrderPaid` проходил мимо проверок (`!=='paid'`, сумма совпала), делал `UPDATE … WHERE status='pending'` (0 строк), но **возвращал `'paid'`**. Робокассе уходил `OK{InvId}`, ретраи прекращались → деньги списаны, заказ остаётся `cancelled`. | `markOrderPaid` (`lib/orders.ts`), `result/route.ts` | ✅ исправлено: явный статус `'cancelled'` (раннее возвращение + 400 «Order cancelled» с логом, без OK). +тесты `orders.markPaid.test.ts`, route. |
| TD-18 | Возврат `'paid'` не привязан к факту обновления строки → при двух параллельных колбэках оба вернули бы `'paid'` (после TD-6 — дубли письма/отгрузки). | `markOrderPaid` | ✅ исправлено: `UPDATE … RETURNING id`, успех судится по числу обновлённых строк; при 0 строк — перечитываем фактический статус (`already_paid`/`cancelled`/`not_found`). +тест гонки. |
| TD-19 | `/result` принимал колбэк с любого IP. Подпись — основная защита, но Робокасса публикует диапазоны IP. | `isAllowedResultIp`, `result/route.ts` | ✅ код готов: allowlist `ROBOKASSA_RESULT_IPS` (CIDR/IP, IPv4 + IPv6-mapped), 403 до проверки подписи. 🔜 ops: заполнить актуальный список из ЛК + Nginx `X-Forwarded-For`. Пусто = выключено. +тесты. |
| TD-20 | Подпись была жёстко на MD5. | `signHex`/`hashAlgo` (`lib/robokassa.ts`) + ЛК | ✅ код готов: алгоритм из `ROBOKASSA_HASH_ALGO` (md5..sha512, дефолт md5), на вход и на проверку. 🔜 ops: выставить `sha256` ЗДЕСЬ и в ЛК ОДНОВРЕМЕННО. +тесты. |
| TD-21.1 | Нет assert при старте, что в проде `ROBOKASSA_TEST_MODE` снят (утечка тест-режима = «оплаты» с `IsTest=1` без денег, связано с TD-7). | `checkPaymentConfig`/`assertPaymentConfigSafe`, `instrumentation.ts` | ✅ исправлено: при `NODE_ENV=production` + `TEST_MODE=true` сервер не стартует (бросает). Заодно валидирует `ROBOKASSA_HASH_ALGO` на старте. +тесты. |
| TD-21.2 | `in_stock` — булев флаг без количества и декремента при оплате → один товар можно продать дважды (overselling). | `orders.ts` / схема / витрина | 🔜 Ф4/витрина: **учёт остатков пока не делаем** (решение от 2026-06-20, см. `decisions.md`). Вместо этого — настраиваемый лимит штук на позицию (напр. ≤5), сейчас захардкожен `MAX_QTY=99`. |

---

## Эксплуатация (выявлено на проде)

Дата: 2026-06-20. Найдено при первом входе в админку на боевом `mavita.ru`.

| ID | Замечание | Где | Серьёзность / статус |
| --- | --- | --- | --- |
| TD-22 | **Вход в админку падал «Неверный Origin» на проде.** `assertSameOrigin` сравнивал заголовок `Origin` (`https://mavita.ru`) с `new URL(request.url).origin`, но `next start` за Nginx строит `request.url` через внутренний HTTP-сервер → `http://mavita.ru`. Протоколы не совпадали, любой POST/PATCH/DELETE админки отклонялся 403. Локально на `localhost` не воспроизводилось. | `assertSameOrigin` (`lib/auth.ts`), `app/api/auth/login` | ✅ исправлено: сверяется **хост** `Origin` с `x-forwarded-host`/`Host` (без протокола) — CSRF-защита сохранена, межсайтовый запрос придёт с другим хостом/без Origin. Тесты обновлены под реальные запросы (с `Host`). Проверено на проде: легитимный вход → 401 на неверный пароль, чужой Origin → 403. |

---

## Ревью реализации админки товаров (после деплоя)

Дата: 2026-06-21. Критическое ревью реализации `docs/specs/done/admin-products.md`
по коду. Базовая часть в порядке (digest-сравнение пароля timing-safe, защита от
фиксации сессии, гарды I8 на всех маршрутах, снапшот I9 в `createOrder` под
`FOR SHARE`, фильтры видимости + 503 без seed, идемпотентная миграция 002) — не
дублируем. Найдены два дефекта, оба исправлены и покрыты тестами.

| ID | Замечание | Где | Серьёзность / статус |
| --- | --- | --- | --- |
| TD-23 | **500 при смене обложки.** `PATCH …/images` обновлял фото по одному в цикле `SET is_cover = (id === coverImageId)`. `uq_product_cover` (partial unique index) проверяется PostgreSQL на каждом statement, поэтому если новая обложка стоит в `orderedImageIds` раньше старой, на её `UPDATE` возникал временный дубль `is_cover=true` → `23505` → необработанный throw → 500. Триггерилось штатным UX (сделать обложкой фото, что сортируется раньше текущей / перетащить новую обложку в начало). Тестом не покрывалось. | `app/api/admin/products/[id]/images/route.ts` | ✅ исправлено: транзакция сперва одним запросом снимает старую обложку, затем назначает новую в цикле; conflict-проверка переведена на числовую сортировку. +регрессионный тест на прежде падавший сценарий (`orderedImageIds:[2,1], cover:2`) и 409 при несовпадении набора. |
| TD-24 | **WebP по факту не загружался.** Парсер размеров читал только контейнер `VP8X` (extended); обычные `VP8 ` (lossy) и `VP8L` (lossless) — большинство WebP — давали `null` → «нераспознаваемое изображение» → 400, хотя спека (§7.3) заявляет `image/webp`. | `app/api/upload/route.ts` → `lib/upload-image.ts` | ✅ исправлено: логика вынесена в чистый `lib/upload-image.ts` (`detectImageType`, `imageDimensions`), размеры читаются для `VP8 `/`VP8L`/`VP8X` с верными бит-смещениями. +параметризованные тесты на все три контейнера. |
