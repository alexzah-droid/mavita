# Spec: <feature-name>

> Создать как `docs/specs/<feature-name>.md` для заметной фичи. Spec — короткий
> контракт перед кодом, не архитектурный трактат.

## Цель владельца

<Зачем это нужно бизнесу или покупателю. 1-3 предложения.>

## Scope / Non-scope

- В scope: `<что делаем>`
- Не в scope: `<что явно не делаем сейчас>`

## Связанные документы

- Архитектура: `<раздел architecture.md>`
- Решения: `<docs/decisions.md #N>`

## Критерии приёмки

- [ ] `<наблюдаемое поведение>`
- [ ] `<наблюдаемое поведение>`

## Инварианты

- Затрагиваемые инварианты из `PROJECT_CORE.md`: `<I1/I2/...>`
- Новый инвариант, если появляется: `<описание>` → добавить в `TESTING_PLAN.md`.

## Тесты

<Какие тесты должны краснеть при регрессии: unit на robokassa.ts, интеграционный на API, e2e на флоу оплаты.>

## Docs to update

- `DOCS.md`: `<новые факты или документы>`
- `docs/decisions.md`: `<если есть решение с последствиями>`

## Rollback

<Как откатить. Если есть необратимая миграция БД — это Пауза 2 до реализации.>
