# PROJECT_CORE — МАВИТА-ШОП

Дата: 2026-06-19

Этот документ — единый источник фактов о проекте. Читается первым в любой рабочей сессии.

---

## 1. Суть проекта

Интернет-магазин свечей МАВИТА: витрина, корзина, оплата через Робокассу, админ-панель для управления товарами и фотографиями.

Стек: Next.js 15 (App Router) + PostgreSQL 16 + Nginx + PM2 на Ubuntu 22.04 VPS (`mavita.ru`, `45.130.147.108`).

---

## 2. Контракт агента

Работай автономно: читай файлы, пиши код, применяй шаблоны, обновляй документы.

**Пауза 1** — остановись и спроси владельца перед:
- любым действием на production-сервере;
- переключением `ROBOKASSA_TEST_MODE=false`;
- любой операцией с реальными деньгами или заказами покупателей.

**Пауза 2** — предложи варианты + рекомендацию перед:
- изменением схемы БД с потерей данных;
- сменой стека или ключевой зависимости;
- добавлением внешней интеграции.

---

## 3. Readiness-гейт

Перед коммитом/деплоем код должен проходить (из каталога `shop/`):

```bash
npm run typecheck && npm test && npm run build
```

> Скрипты: `typecheck` = `tsc --noEmit`, `test` = `vitest run`. Отдельного `lint` пока нет.

---

## 4. Единый источник фактов

| Факт | Документ |
| --- | --- |
| Архитектура, стек, схема БД | `architecture.md` |
| План развития по фазам | `ROADMAP.md` |
| Реестр документов | `DOCS.md` |
| Дизайн-система, цвета, шрифты | `brand.md` |
| Инварианты и контракт агента | этот файл |
| Журнал решений | `docs/decisions.md` |
| Спецификации фич | `docs/specs/<feature>.md` |
| Окружения (после деплоя) | `docs/environments.md` |
| Operations runbook (после деплоя) | `docs/operations.md` |

---

## 5. Инварианты

Нарушение любого — стоп, Пауза 2.

| ID | Инвариант |
| --- | --- |
| I1 | Подпись Робокассы считается **только** на сервере в `lib/robokassa.ts`. Password1/Password2 — только в `.env`. |
| I2 | Цены хранятся в копейках как `INTEGER`. FLOAT для цен запрещён. |
| I3 | `POST /api/robokassa/result` меняет статус заказа **только** после успешной проверки MD5-подписи. |
| I4 | Статус заказа меняется только через API, не прямым `UPDATE` в БД. |
| I5 | Фото загружается только через admin-only `POST /api/upload`; файл и запись в `product_images` создаются компенсируемо-атомарно, без анонимных или непривязанных файлов. |
| I6 | `index.html` не редактируется вручную — это сгенерированный бандл КП. |
| I7 | `.env` с реальными секретами никогда не коммитится. |
| I8 | Каждый `/api/admin/**`, `/api/upload` и каждая защищённая `/admin/**`-страница выполняет работу только после `requireAdminApi()` / `requireAdminPage()`; исключения — login/logout. Изменяющие запросы проверяют same-origin. Пароль/секрет — только в `.env`, сравнение — `timingSafeEqual` SHA-256 digest одинаковой длины. |
| I9 | Цена позиции заказа — это `effectivePrice()` по заблокированному в транзакции срезу БД в момент создания заказа. Видимость/цена с клиента игнорируются; `hidden`-товар купить нельзя. |
| I10 | Итог заказа равен серверному snapshot `товары + доставка`; клиент не задаёт цену доставки. Платёжный и исполнительский статусы разделены: ни один ручной шаг отгрузки не может установить `paid`. |

---

## 6. Текущий статус

Источник статуса фаз и чек-листов — **`ROADMAP.md`** (здесь не дублируется, чтобы не расходиться).

Кратко на 2026-06-20: Ф0–Ф3 и Ф5 готовы (mavita.ru работает, `ROBOKASSA_TEST_MODE=true`, тестовый платёж прошёл). Ф4 (админка) — **первый компонент реализован и на проде** (авторизация + управление товарами, фото, видимостью и скидками: [docs/specs/admin-products.md](docs/specs/admin-products.md)); инварианты I8/I9 действуют. Второй компонент (заказы + доставка СДЭК, I10) — спроектирован, не начат: [docs/specs/admin-orders.md](docs/specs/admin-orders.md). После деплоя исправлен CSRF за прокси (TD-22).

**Следующий шаг:** компонент 2 (заказы) по спеке `admin-orders.md`; параллельно — переключение Робокассы на боевой режим (Пауза 1).
